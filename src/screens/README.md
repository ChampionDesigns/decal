# src/screens/

`live-screen.js`, `selector-screen.js`, `editor-screen.js`, `settings-screen.js`,
`history-screen.js`. Also components, but layout-only.

A route change swaps which screen is mounted inside `<app-root>`. The History Viewer
is a route, not a `display:flex` toggle.
