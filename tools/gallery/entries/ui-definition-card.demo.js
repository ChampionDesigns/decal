/**
 * A gallery entry names one module, so an entry whose states mount more than one custom
 * element points at a loader instead of at a component. This one registers the card and
 * the button its heading row can carry.
 */

import 'src/components/ui-definition-card.js';
import 'src/components/ui-button.js';
