/** CONTROL — the `EDITOR_EDIT` shape: a frozen table of event names, spelled once. */
export const EDITOR_EDIT = Object.freeze({
    STEP_CHANGE: 'step-change',
    VALUE_COMMIT: 'value-commit',
    EXIT_REMOVE: 'exit-remove',
});
