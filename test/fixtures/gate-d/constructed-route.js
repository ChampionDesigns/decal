// CANARY — coverage-constructed-path.
// Assembled from fragments, in a file the table does not declare as a route builder.
export function sleepUrl(base, machineId) {
    return `${base}/machine/${machineId}/sleep`;
}
